{
	"version": 2,
	"buildCommand": "npm start",
	"installCommand": "npm install",
	"builds": [
		{
			"src": "./index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/"
		}
	]
}
